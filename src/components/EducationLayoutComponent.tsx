import React from "react";

interface EducationLayoutComponentProps {
  children: React.ReactNode;
}

const EducationLayoutComponent = ({ children }: EducationLayoutComponentProps) => {
  return <>{children}</>;
};

export default EducationLayoutComponent;
